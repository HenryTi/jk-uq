-- 池
ID Pool CONST (
    id,
    KEY name char(50),
    description char(100),
)
+ (name, description)
values
('pool', 'weak bound customers')
;

--客户与池的关系
IX CustomerPool (
    ix,  -- ID Pool,
    xi   -- ID Customer,
);
